import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
    images: {
        domains: ['dummyimage.com', 'source.boringavatars.com', 'images.unsplash.com'],
    },
    /* config options here */
};

export default nextConfig;
